version: "2"

services:
    fpm:
        image: nitra/php
        volumes:
            - .:/var/www
 
    nginx:
        image: nitra/nginx:cors
        ports:
            - 8055:80
        volumes_from:
            - fpm:ro

    mysql:
        image: nitra/mariadb
        environment:
            MARIADB_PASS: admin
            MARIADB_USER: admin

    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        ports:
            - 8003:80
        environment:
            PMA_HOST: mysql